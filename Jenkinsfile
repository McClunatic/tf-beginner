pipeline {
    agent any

    stages {
        stage('Train') {
            steps {
                echo 'Training..'
                sh 'whoami'
                sh 'ls -l'
            }
        }
    }
}